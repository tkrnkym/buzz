[package]
name = "nuxx-admin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Operator CLI for Nuxx relay administration"

[[bin]]
name = "nuxx-admin"
path = "src/main.rs"

[dependencies]
nuxx-db = { workspace = true }
nuxx-core = { workspace = true }
nuxx-auth = { workspace = true }
nuxx-pubsub = { workspace = true }
nuxx-search = { workspace = true }
nuxx-audit = { workspace = true }
nuxx-workflow = { workspace = true }
nuxx-media = { workspace = true }
nostr = { workspace = true }
tokio = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
hex = { workspace = true }
deadpool-redis = { workspace = true }
# Redis TLS (rediss://, e.g. ElastiCache) uses rustls, which needs a process
# CryptoProvider installed at startup. The workspace redis TLS feature compiles
# both aws-lc-rs and ring in transitively, so rustls can't auto-select one — we
# install ring explicitly in main(). Mirrors nuxx-relay's setup.
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
tracing = { workspace = true }
sqlx = { workspace = true }
url = { workspace = true }
clap = { version = "4", features = ["derive"] }

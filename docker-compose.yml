name: nuxx

services:
  postgres:
    image: postgres:17-alpine
    container_name: nuxx-postgres
    environment:
      POSTGRES_USER: nuxx
      POSTGRES_PASSWORD: nuxx_dev
      POSTGRES_DB: nuxx
      PGDATA: /var/lib/postgresql/data
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - nuxx-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nuxx"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.nuxx.service: "postgres"
      com.nuxx.env: "dev"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: nuxx-redis
    ports:
      - "6379:6379"
    networks:
      - nuxx-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.nuxx.service: "redis"
      com.nuxx.env: "dev"
    restart: unless-stopped

  adminer:
    image: adminer:latest
    container_name: nuxx-adminer
    ports:
      - "8082:8080"
    networks:
      - nuxx-net
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    deploy:
      resources:
        limits:
          memory: 64m
    labels:
      com.nuxx.service: "adminer"
      com.nuxx.env: "dev"
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: nuxx-keycloak
    command: start-dev --http-port=8080
    environment:
      KC_DB: dev-mem
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8180:8080"
    networks:
      - nuxx-net
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200 OK'"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.nuxx.service: "keycloak"
      com.nuxx.env: "dev"
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    container_name: nuxx-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: nuxx_dev
      MINIO_ROOT_PASSWORD: nuxx_dev_secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - nuxx-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
    labels:
      com.nuxx.service: "minio"
      com.nuxx.env: "dev"
    restart: unless-stopped

  minio-init:
    image: minio/mc:latest
    container_name: nuxx-minio-init
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - nuxx-net
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 nuxx_dev nuxx_dev_secret &&
        mc mb --ignore-existing local/nuxx-media &&
        mc anonymous set none local/nuxx-media
      "
    labels:
      com.nuxx.service: "minio-init"
      com.nuxx.env: "dev"
    restart: "no"

  prometheus:
    image: prom/prometheus:latest
    container_name: nuxx-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - nuxx-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.nuxx.service: "prometheus"
      com.nuxx.env: "dev"
    restart: unless-stopped

volumes:
  postgres-data:
    name: nuxx-postgres-data
    labels:
      com.nuxx.volume: "postgres"
  minio-data:
    name: nuxx-minio-data
    labels:
      com.nuxx.volume: "minio"
  prometheus-data:
    name: nuxx-prometheus-data
    labels:
      com.nuxx.volume: "prometheus"

networks:
  nuxx-net:
    name: nuxx-net
    driver: bridge
    labels:
      com.nuxx.network: "dev"

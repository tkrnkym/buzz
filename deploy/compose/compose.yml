name: nuxx-prod

services:
  relay:
    image: ${NUXX_IMAGE:-ghcr.io/tkrnkym/nuxx:main}
    env_file:
      - .env
    environment:
      NUXX_BIND_ADDR: 0.0.0.0:3000
      NUXX_HEALTH_PORT: "8080"
      NUXX_METRICS_PORT: "9102"
      DATABASE_URL: postgres://${POSTGRES_USER:-nuxx}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-nuxx}
      REDIS_URL: redis://:${REDIS_PASSWORD:?set REDIS_PASSWORD}@redis:6379
      NUXX_S3_ENDPOINT: http://minio:9000
      # Docker DNS resolves `minio`, not arbitrary `<bucket>.minio` hosts.
      NUXX_S3_ADDRESSING_STYLE: path
      NUXX_S3_ACCESS_KEY: ${NUXX_S3_ACCESS_KEY:?set NUXX_S3_ACCESS_KEY}
      NUXX_S3_SECRET_KEY: ${NUXX_S3_SECRET_KEY:?set NUXX_S3_SECRET_KEY}
      NUXX_S3_BUCKET: ${NUXX_S3_BUCKET:-nuxx-media}
      NUXX_GIT_REPO_PATH: /data/git
      NUXX_AUTO_MIGRATE: ${NUXX_AUTO_MIGRATE:-false}
      NUXX_GIT_CONFORMANCE_PROBE: ${NUXX_GIT_CONFORMANCE_PROBE:-true}
    ports:
      - "${NUXX_HTTP_PORT:-3000}:3000"
    volumes:
      - nuxx-git-data:/data/git
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
    # Probe /_readiness over /dev/tcp because the runtime image has bash but no curl/wget/socat.
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bash -ec 'exec 3<>/dev/tcp/127.0.0.1/8080; printf \"GET /_readiness HTTP/1.1\\r\\nHost: 127.0.0.1\\r\\nConnection: close\\r\\n\\r\\n\" >&3; grep -q \"200 OK\" <&3'",
        ]
      interval: 10s
      timeout: 3s
      retries: 12
      start_period: 30s
    restart: unless-stopped
    networks:
      - nuxx-net

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-nuxx}
      POSTGRES_USER: ${POSTGRES_USER:-nuxx}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - nuxx-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped
    networks:
      - nuxx-net

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${REDIS_PASSWORD:?set REDIS_PASSWORD}"]
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?set REDIS_PASSWORD}
    volumes:
      - nuxx-redis-data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$${REDIS_PASSWORD}\" ping | grep -q PONG"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    restart: unless-stopped
    networks:
      - nuxx-net

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${NUXX_S3_ACCESS_KEY:?set NUXX_S3_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${NUXX_S3_SECRET_KEY:?set NUXX_S3_SECRET_KEY}
    volumes:
      - nuxx-minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped
    networks:
      - nuxx-net

  minio-init:
    image: minio/mc:RELEASE.2025-08-13T08-35-41Z
    depends_on:
      minio:
        condition: service_healthy
    environment:
      NUXX_S3_ACCESS_KEY: ${NUXX_S3_ACCESS_KEY:?set NUXX_S3_ACCESS_KEY}
      NUXX_S3_SECRET_KEY: ${NUXX_S3_SECRET_KEY:?set NUXX_S3_SECRET_KEY}
      NUXX_S3_BUCKET: ${NUXX_S3_BUCKET:-nuxx-media}
    entrypoint: >
      /bin/sh -euc '
        mc alias set local http://minio:9000 "$${NUXX_S3_ACCESS_KEY}" "$${NUXX_S3_SECRET_KEY}"
        mc mb --ignore-existing "local/$${NUXX_S3_BUCKET}"
        mc anonymous set none "local/$${NUXX_S3_BUCKET}"
      '
    restart: "no"
    networks:
      - nuxx-net

volumes:
  nuxx-postgres-data:
    labels:
      com.nuxx.volume: postgres
  nuxx-redis-data:
    labels:
      com.nuxx.volume: redis
  nuxx-minio-data:
    labels:
      com.nuxx.volume: minio
  nuxx-git-data:
    labels:
      com.nuxx.volume: git

networks:
  nuxx-net:
    driver: bridge
    labels:
      com.nuxx.network: production

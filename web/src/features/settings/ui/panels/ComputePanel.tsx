import { MeshComputeSettings } from "@/features/compute/ui/MeshComputeSettings";

/** Sharing this machine's inference capacity. One subject, so one component. */
export function ComputePanel() {
  return <MeshComputeSettings />;
}
